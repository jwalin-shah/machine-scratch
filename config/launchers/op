#!/usr/bin/env bash
# OpenCode — Pioneer AI. API key via secret-cache exec.
set -euo pipefail
export OPENCODE_CONFIG="$HOME/.config/opencode/profiles/op.json"
exec "$HOME/.local/bin/secret-cache" exec -- /opt/homebrew/bin/opencode "$@"
