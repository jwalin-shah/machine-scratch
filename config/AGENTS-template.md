# ── Agent instructions template ──
# Copy to projects/<name>/AGENTS.md (symlink as CLAUDE.md)

# <Project Name>

## What
One-line description.

## Tech
- Language: Python 3.x / Go / TypeScript / Rust
- Framework:
- Package manager: uv / pnpm / cargo
- Test runner: pytest / vitest / go test

## Entry points
-

## Conventions
- Use `fastedit` for editing
- Use `rtk` for reading/grepping
- Use `llm-tldr` for code structure and search
